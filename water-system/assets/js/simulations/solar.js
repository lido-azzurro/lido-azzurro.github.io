export const solar = {
  init(root){
    root.innerHTML = `
      <div class="muted" style="margin-bottom:10px">Simulation 2 — Solar supply (module scaffold)</div>
      <div class="mono">Next step: implement P = G·A·η + day/cloud cycle + pump availability indicator.</div>
    `;
  }
};
