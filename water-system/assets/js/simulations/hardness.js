export const hardness = {
  init(root){
    root.innerHTML = `
      <div class="muted" style="margin-bottom:10px">Simulation 4 — Hardness analysis (module scaffold)</div>
      <div class="mono">Next step: Ca/Mg→CaCO₃ eq hardness, classification bands, scale risk indicator.</div>
    `;
  }
};
