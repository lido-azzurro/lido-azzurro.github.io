export const filtration = {
  init(root){
    root.innerHTML = `
      <div class="muted" style="margin-bottom:10px">Simulation 3 — Filtration (module scaffold)</div>
      <div class="mono">Next step: turbidity reduction vs thickness + flow reduction + visual clarity animation.</div>
    `;
  }
};
