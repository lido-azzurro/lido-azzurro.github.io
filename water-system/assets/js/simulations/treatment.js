export const treatment = {
  init(root){
    root.innerHTML = `
      <div class="muted" style="margin-bottom:10px">Simulation 5 — Chemical treatment (module scaffold)</div>
      <div class="mono">Next step: treatment type + dosage→hardness reduction + side effects (pH shift/sludge).</div>
    `;
  }
};
